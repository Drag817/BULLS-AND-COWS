#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

using namespace std;

void swap(int& x, int& y);
void Random(int* A, int N);
void AnswerCheck(int* A, int* B, int& bull, int& cow, int N);
int Menu();

#endif
